function calculateNumbers(var1, var2)   {
    var sumTotal = var1 + var2;
    var productTotal = var1 * var2;
    var quotientTotal = var1 / var2;
    var differenceTotal = var1 - var2;
    return [sumTotal, productTotal, quotientTotal, differenceTotal];
}